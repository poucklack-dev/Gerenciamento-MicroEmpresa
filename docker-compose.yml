services:
  web:
    build: .
    container_name: patagonia_web
    # Monta o código-fonte e os uploads para desenvolvimento local.
    volumes:
      - ./:/app
      - ./uploads:/tmp/uploads
    ports:
      - "8080:8080"
    environment:
      ENV: development
      STORAGE_DRIVER: local
      LOCAL_STORAGE_PATH: /tmp/uploads
      SECRET_KEY: dev-secret-key-change-me
      ENABLE_DEBUG_ROUTES: "0"
      DB_HOST: db
      DB_PORT: "5432"
      DB_NAME: patagonia
      DB_USER: patagonia
      DB_PASS: patagonia
    depends_on:
      - db

  db:
    image: postgres:15-alpine
    container_name: patagonia_db
    volumes:
      - postgres_data:/var/lib/postgresql/data/
      - ./schema_patagonia.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
    environment:
      POSTGRES_DB: patagonia
      POSTGRES_USER: patagonia
      POSTGRES_PASSWORD: patagonia

volumes:
  postgres_data:
